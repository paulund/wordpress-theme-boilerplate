/**
 * @package WordPress-Theme-Boilerplate
 */

var $ = jQuery.noConflict();

jQuery(document).ready(function( $ ){
    namespace.init();
});

var namespace =
{
    init: function()
    {
        // do stuff
    }
}